<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a href="#" class="navbar-brand">
        GuiaPress
    </a>

    <ul class="navbar-nav mr-auto">
        <li class="nav-item">
            <a href="/" class="nav-link">Página Inicial</a>
        </li>
        <li class="nav-item">
            <a href="/about" class="nav-link">Sobre</a>
        </li>
        <li class="nav-item">
            <div class="btn-group">
                <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    Categorias
                </button>
                <ul class="dropdown-menu scrollable-menu" role="menu">

                    <% categories.forEach(category =>{ %>
                    <li>
                        <a href="/articles/page/1?category=<%= category.slug %>"
                            class="dropdown-item"><%= category.title %></a>
                    </li>
                    <% }) %>

                </ul>
            </div>
        </li>
    </ul>
    <ul class="navbar-nav ml-auto">

        <li class="nav-item">
            <div class="btn-group dropleft">
                <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    Login
                </button>
                <div class="dropdown-menu">
                    <form class="px-4 py-3" method="POST" action="/authenticate">
                        <div class="form-group">
                            <label for="email">Email</label>
                            <input type="text" name="email" class="form-control" id="email"
                                placeholder="email@example.com">
                        </div>
                        <div class="form-group">
                            <label for="password">Senha</label>
                            <input type="password" name="password" class="form-control" id="password"
                                placeholder="Informe sua senha">
                        </div>
                        <button type="submit" class="btn btn-primary">Sign in</button>
                    </form>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="/admin/users/new">Novo por
                        aqui? Cadastre-se</a>
                </div>
            </div>
        </li>
    </ul>
</nav>

<script>
    function ChangeMenu(option) {
        const sigin = document.querySelector("#signin")
        const sigup = document.querySelector("#signup")

        if (option.id == "sigin-menu") {
            sigin.classList.add("sr-only")
            sigup.classList.remove("sr-only")
        } else {
            sigin.classList.remove("sr-only")
            sigup.classList.add("sr-only")
        }
    }
</script>